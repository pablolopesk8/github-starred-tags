const should = require('should'); // eslint-disable-line
const { GetUserByGithubUser } = require('../../src/services/users.service');

describe('Users Service Test', () => {
    describe('Get User From DB or Github', () => {
        it('Should be able to get user data with a valid github user didnt save previous', async () => {
        });

        it('Should be able to get user data with a valid github user saved previous', async () => {
        });
    });
});